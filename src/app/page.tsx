import { Button } from "@/components/ui/button";
import React from "react";

const Home = () => {
  return (
    <div>
      <Button variant={"outline"}>ТЕСТ</Button>
    </div>
  );
};

export default Home;
